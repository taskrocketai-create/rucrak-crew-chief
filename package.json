{
  "name": "rucrak-field-support",
  "version": "1.0.0",
  "private": true,
  "description": "rucRak Crew Chief customer support chatbot — static frontend + Vercel serverless API proxy to Claude.",
  "scripts": {
    "test": "node --test tests/chat.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
